# Bob_MREF2_MCP Requirements
# Maximo Real Estate and Facilities - OSLC Integration

# Core dependencies
requests>=2.31.0
urllib3>=2.0.0

# Optional dependencies for enhanced functionality
python-dateutil>=2.8.2

# Note: MCP SDK (mcp>=0.9.0) is provided by Bob's runtime environment
# This server is designed to run within Bob/Claude Desktop, not standalone
# The MCP framework is automatically available when running as an MCP server